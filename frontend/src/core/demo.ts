/**
 * Демо-режим: интерфейс работает на фикстурах, без бэкенда.
 *
 * Включается через NEXT_PUBLIC_DEMO=1. Нужен для трёх вещей: разработка
 * интерфейса без поднятия Postgres, Redis и ключей провайдеров; демонстрация
 * продукта; воспроизводимые скриншоты.
 *
 * СТАТУС: не реализован. Заглушка возвращает явную ошибку вместо пустых данных —
 * молчаливый пустой ответ выглядел бы как «в базе ничего нет» и увёл бы отладку
 * в неверную сторону.
 *
 * Что нужно сделать:
 *  1. Детерминированный генератор компаний (сид фиксированный, иначе сервер и
 *     клиент отрисуют разные данные и получим ошибку гидратации).
 *  2. Обработчики путей, повторяющие поведение бэкенда: фильтры и пагинация
 *     `/leads`, канбан `/crm/board`, счётчики `/stats/dashboard`, разбор запроса
 *     `/chat`, генерация оффера, список сообщений.
 *  3. Изменения (статус, мониторинг) держать в модульном состоянии, чтобы
 *     действия в интерфейсе были видимы до перезагрузки страницы.
 *  4. Искусственная задержка, иначе скелетоны и состояния загрузки не проверить.
 */

import type { RequestOptions } from "./api";

export class DemoNotImplementedError extends Error {
  constructor(path: string) {
    super(
      `Демо-режим ещё не реализован (запрос ${path}). ` +
        `Выключите NEXT_PUBLIC_DEMO или запустите бэкенд.`,
    );
    this.name = "DemoNotImplementedError";
  }
}

export async function demoRequest<T>(
  path: string,
  _options: RequestOptions = {},
): Promise<T> {
  throw new DemoNotImplementedError(path);
}
